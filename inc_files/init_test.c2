module tests;
import stdio as io;
///import your modules here
///example:
/*
** import mymodule as lib;
** import another_mod local;
** import third_mod as lib2 local;
*/

// your tests here
public func bool eq(i32 a, int32 b)
{
	return a == b;
}


// add them to main
public func i32 main(i32 argc, char** argv)
{
	i32 test_num = 0;
	io.printf("test %d %s", ++test_num, eq(5,5) ? "passed" : "failed");

	return 0;
}
